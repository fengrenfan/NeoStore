# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Node
node_modules/
dist/
.next/
out/
*.tsbuildinfo

# Secrets & local state
.env
.env.local
*.pem
pgdata/
caddy_data/

# Compose's convention file for per-host overrides — published ports, package
# mirrors, the public origin. Every host has different answers, so it stays out
# of the repository and is written once per server.
docker-compose.override.yml

# Smoke-test databases (one per run, named smoke-<pid>.db)
smoke*.db
smoke*.db-wal
smoke*.db-journal

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp
