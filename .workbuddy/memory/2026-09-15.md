# 2026-09-15

## 调研：GitHub 头部跨境独立站开源项目

用 GitHub API 实测（不抄榜单文章，那些数据互相矛盾），得到 star / 协议 / 语言 / 活跃度：

| # | 项目 | Stars | 语言 | 协议 | API |
|---|---|---|---|---|---|
| 1 | medusajs/medusa | 36,304 | TypeScript/Node | MIT(core)+EE | REST |
| 2 | bagisto/bagisto | 28,114 | PHP/Laravel | MIT | REST + GraphQL |
| 3 | saleor/saleor | 23,327 | Python/Django | BSD-3 | 仅 GraphQL |
| 4 | spree/spree | 15,697 | Ruby on Rails | BSD-3 | REST + TS SDK |
| 5 | vendurehq/vendure | 8,438 | TypeScript/NestJS | GPL-3 | GraphQL |

参考轻量路线：vercel/commerce 14,258（MIT）、yournextstore 5,498（MIT，Next.js+Stripe）。

**共性结论（本项目复用的核心）**
- 领域链路一致：Product → Variant → SKU → Price(按 Region/Channel) → Inventory(按 Warehouse)
  → Cart → Checkout → Order → Fulfillment → Return。
- 四层分层 + Admin 永远是独立第二个前端。
- 扩展机制只有三种：进程内 Plugin、进程外 App(webhook)、可替换 Module。
- 中间件标配：PostgreSQL + Redis + 专用搜索 + Docker Compose。
- 订单行必须快照商品名/价，不能外键回查（否则改价污染历史订单）——五家全这么做。
- 跨境三刚需：币种抽象成渠道/区域维度、i18n 原生字段或外挂 CMS、支付网关插件化。

## 新项目：NeoStore（轻量单店 DTC 跨境独立站）

用户确认的决策：单店 DTC 形态 / FastAPI + React / 先做通用多语言框架 /
方案 A 前端结构（Next.js 店面 + 独立 Vite 后台）/ PostgreSQL / 首期最小可用闭环。

设计文档已落地并提交：
`docs/superpowers/specs/2026-09-15-neostore-design.md`
git 已 init，首个 commit `60fd0bd`。

关键设计决定：
- i18n 用独立翻译表（`*_translation` + UNIQUE(entity_id, locale)），不用 JSONB 内嵌字段。
- 多币种：显式定价优先，缺失时按汇率换算兜底；换算结果按目标币种 decimal_places 取整。
- Region = 币种 + 语言集合 + 税率 + 可用支付方式的聚合。
- 金额一律 Decimal，计算收敛到 `pricing.calculate()` 单一入口。
- 库存用 `SELECT ... FOR UPDATE` 行锁 + reserved 预占防超卖。
- 下单接口支持 `Idempotency-Key` 防重复下单。
- domain 层禁止 import fastapi / 直接拿 Session，持久化走 repository 接口。

状态：**设计待用户评审**，评审通过后进入实施计划（M0–M4）。

## 实施：M0–M4 全部落地（同日续）

按 `docs/superpowers/plans/2026-09-15-neostore-mvp.md` 的 18 个 Task 执行完毕：

- **后端**：FastAPI + SQLAlchemy 2.0 + Alembic 全量领域（i18n / currency / region / catalog /
  pricing / inventory / cart / order / payment），store 与 admin 两套 API + JWT 鉴权。
  测试 150 passed / 1 skipped（跳过的那个是 PostgreSQL 行锁超卖用例，SQLite 上跑不了）。ruff 全绿。
- **顾客端** `storefront/`：Next.js 15 App Router + `[locale]` 路由 + SSR + sitemap/robots/JSON-LD/hreflang，
  standalone 产物 + 三阶段 Dockerfile。
- **后台** `admin/`：Vite + React 19 + TanStack Query + React Router（basename `/admin`），
  概览 / 商品 / 订单 / 库存 / 设置五页，nginx 托管在 `/admin/`。
- **编排**：docker-compose 六个服务 + Caddy 按前缀分流（`/api` → api、`/admin` → admin、其余 → storefront）。

### 今天踩到并修掉的真问题（都值得记住）

1. **冒烟测试被"陈旧数据库"骗过**：`smoke.sh` 原来删共享的 `smoke.db`，一旦删除没落地
   （被拒绝、残留 `-wal`、旧进程抱着旧 inode），本次运行就继承了上一轮的订单，
   于是 checkout 返回的是上轮那笔**已支付**订单，断言全在描述错误的世界。
   修法：每轮用唯一 `smoke-<pid>.db` + 断言"0 订单 3 商品" + 端口被占用时直接退出。
2. **时间戳时区**：SQLite 的 `DateTime(timezone=True)` 回来是 naive，浏览器把
   `2026-09-15T07:51:52` 当本地时间读，CST 下显示差 8 小时。加了 `app/core/serialization.py`
   的 `UtcDatetime`（`+00:00`），PostgreSQL 与 SQLite 行为一致。
3. **`docker compose` 里 `app.seed --if-empty` 这个参数并不存在**，argparse 会非零退出 →
   api 容器起不来。已删（seed 本身幂等）。
4. 沙箱内 npm install 会被文件系统 broker 拒绝 → `scripts/sandbox-fs-guard.cjs`（`node -r` 预加载，
   限制 mkdir/rename/symlink 并发）是唯一可行路径。

### 验证方式（以后照做）

- `bash scripts/smoke.sh`：全链路 40 项，含两个前端的静态渲染。
- `node scripts/admin-check.cjs <后台地址> <待支付订单号>`：真实 Chromium 把后台走一遍
  （登录 → 概览 → 商品 → 库存调整 → 设置 → 订单状态流转），15 项断言 + 零 console 报错。

## 推送远端：github.com/fengrenfan/NeoStore

仓库是用户在网页端建的（默认分支 `1.0.1`，只有一个 20 字节的占位 README），
本地 10 个提交已全部推上去，两个分支 `1.0.1`（默认）与 `main` 指向同一提交 `f88c791`。

- **github.com 直连被封**（curl 000，只有 api.github.com 200），走 REST API 推。
- **`~/.git-credentials` 里的 PAT 已失效（401）**；有效凭据在 **macOS 钥匙串**
  （全局 `credential.helper = osxkeychain`），用
  `git credential-osxkeychain get` 取，权限含 `repo`。这条已写进 github-api-push 技能。
- 为此给该技能补了 `scripts/push_history_via_api.py`：整条历史按 root→HEAD 复刻
  （远端为空时 `push_via_api.py` 不够用，父提交在服务端不存在会 400），支持断点续推、
  二进制走 blob API、分支不存在时用 `POST /git/refs` 新建。
- 校验：两分支 head/tree 与本地一致，**192 个文件逐个比 blob sha 零差异**。
- 顺带给 admin 加了 favicon（消除控制台那个 404）。
- 又补了 `backend/.dockerignore`：Dockerfile 只 COPY 了 pyproject/app/alembic，但 Docker 仍会
  把整个上下文打包送给 daemon，本地那个 127 MB 的 `.venv` 每次构建白传一遍。

## 部署到服务器 /home/app/store

服务器现状（先查再动，避免踩别人）：

- 宿主机 nginx 独占 **80/443**，已有三个站点：`xiaodigua.shop`、`blog.xiaodigua.shop`、
  `quant.xiaodigua.shop`，证书都在 `/etc/letsencrypt/live/<域>/`（Certbot 管的）。
- 已占端口：80/443、8088(blog-nginx)、8090(quant-web)、18000(quant-engine)、
  18081(quant-backend)、8080/8081、3001、9090。→ NeoStore 只能换一套端口。
- **内存本来就紧**：3.7 G 物理内存只剩 1.28 G，swap 6 G 已用掉 2.7 G。
  Docker 有 ~11 GB 可回收空间（镜像 10.4 GB / 构建缓存 5.7 GB），磁盘 81% 但够用。

落地方式：

- **代码用 git bundle 传**（沙箱和服务器都连不上 github.com）：`git bundle create --all`
  → scp → 服务器 `git clone /tmp/neostore.bundle .`。校验：193 个跟踪文件、HEAD `3df2cd4`、
  `git status` 干净、`git fsck` 无输出。
- `/home/app/store` 原本是 root 的空目录，先 `chown ubuntu:ubuntu`。ubuntu 在 docker 组，
  不用 sudo 就能跑 compose。
- **写了两份服务器专属文件（都在 .gitignore 里，不入库）**：
  - `backend/.env`：随机 `JWT_SECRET`（64 hex）+ 随机管理员密码（19 位），
    `CORS_ORIGINS` 含 `http://124.222.204.236:8091`。
  - `docker-compose.override.yml`：Caddy 改到 **8091:80**，`api/storefront/admin` 只绑
    127.0.0.1（18082 / 3002 / 8092），db/redis/worker 不发布端口。
- **踩坑：Compose 对 `ports` 这类序列是「合并」不是「替换」**。override 里直接写 `ports:`
  会变成 80+443+8091 三个都绑，Caddy 必然撞宿主机 nginx 起不来。必须用 `!override`
  标签（Compose ≥ 2.24.4，线上是 2.40.3 支持）整段替换。
- **storefront 的浏览器端地址故意留空**：`NEXT_PUBLIC_API_BASE_URL: ""`，
  于是 `baseUrl()` 返回 `""`，fetch 拼出 `/api/v1/...` 相对路径 → 同源。
  好处是同一个镜像放到端口 / 子域名 / HTTPS 后面都不用重新构建。
  （`??` 不会把空字符串当 nullish，所以空串能穿过默认值。）
  服务端组件仍走 `API_BASE_URL=http://api:8000`，不受影响。这条和 admin 那个
  「同源 `/api/v1`，一份产物走天下」的设计是一致的。
- **`store.xiaodigua.shop` 目前没有 DNS 记录**，所以暂时只能 `http://124.222.204.236:8091`。
  想上 HTTPS 子域名需要：DNSPod 加 A 记录 → 加 nginx 站点 → certbot 签证书。
- 构建用 `COMPOSE_PARALLEL_LIMIT=1` 串行，避免几个 node/python 构建同时抢那 1.2 G 内存。

## 这台服务器构建镜像的两个大坑（下次直接照做）

**一、Docker Hub 直连不通，daemon.json 里的源一半是死的。**

- `/etc/docker/daemon.json` 写着 ustc / 网易 / 腾讯云内网三个源，但 **ustc 和网易
  早就停服了**，`registry-1.docker.io` 本身也不通（curl 超时）。构建会卡在拉基础镜像上
  十几分钟毫无进展，而且因为 `| tail` 缓冲，**看不到任何报错**——只有 `docker images`
  里什么都没有才能看出来。
- 可用的：**`mirror.ccs.tencentyun.com`**（腾讯云内网，200，最快）、
  `docker.m.daocloud.io`、`docker.1panel.live`、`hub.rat.dev`。
- **不要为此重启 dockerd**——机器上还有 blog / quant / cosmetics 三套在跑，重启会一起抖。
  改用「预拉取 + 打标签」：`docker pull mirror.ccs.tencentyun.com/library/<img>` 然后
  `docker tag` 成官方名，构建时就不再访问 registry。
- 需要的 5 个基础镜像：`postgres:16-alpine` `python:3.13-slim` `node:22-alpine`
  `nginx:1.27-alpine` `caddy:2-alpine`（`redis:7-alpine` 本来就有）。

**二、包源：apt 25 KB/s，pypi.org 直接不通。**

- `deb.debian.org` 从这台机器下载只有 **~25 KB/s**（装 build-essential 92 MB 要一小时）；
  **`pypi.org` 完全不可达**——也就是说后端镜像按原样永远不会构建成功；
  `registry.npmjs.org` 通，但只有 5.5 MB/s。
- 腾讯云内网源实测：pypi **55 MB/s**、npm 18 MB/s、debian 200。
  完整取值（写进 override）：
  - `APT_MIRROR: mirrors.tencentyun.com`（只给主机名，让 `/debian` 和 `/debian-security` 都命中）
  - `PIP_INDEX_URL: https://mirrors.tencentyun.com/pypi/simple/`
  - `PIP_TRUSTED_HOST: mirrors.tencentyun.com`
  - `NPM_REGISTRY: https://mirrors.tencentyun.com/npm/`
- Dockerfile 里做成 **build arg 且默认指向官方源**，服务器用 override 注入 →
  仓库保持可移植，不会把国内源焊死进代码。
- python:3.13-slim 是 Debian 13 (trixie)，apt 源在
  `/etc/apt/sources.list.d/debian.sources`（deb822 格式，`URIs:` 行），
  sed 替换 `deb.debian.org` 即可。

## storefront 生产构建挂了：三个真 bug

`next build` 在服务器上失败：`/sitemap.xml` 和三个 `/[locale]/cart` 各卡满 60 秒预渲染
超时、重试三次，整个构建连带挂掉。**注意这是这个前端第一次真正跑生产构建**——
之前的 smoke 用的是 dev server，所以问题一直藏着。

线索：`robots.ts`（同为静态路由、同样调 `siteUrl()`）构建正常，`sitemap.ts` 不行，
**唯一差别是后者 await 了一次 fetch**。

三个原因，都是真的：

1. **`cart/page.tsx` 是全 `(shop)` 下唯一漏了 `force-dynamic` 的页面。**
   其余四个（home / products / products\[slug\] / checkout）都声明了，
   `lib/server.ts` 的注释也白纸黑字写着「依赖它的路由都标了 force-dynamic」。
   购物车是 per-visitor（布局要读 cookie 定区域定价），本来绝不能预渲染成共享壳。
2. **构建期从没设过 `API_BASE_URL`。** compose 里的 `environment` 只在运行时生效，
   构建阶段没有 → 服务端 `baseUrl()` 落到公开地址，而生产环境那是**空字符串** →
   `fetch` 拿到相对路径 → 抛**裸 TypeError**，而 `tolerantDuringBuild` 只认
   `ApiError(NETWORK_ERROR)`，于是不兜底、直接把预渲染 worker 拖死。
   修法：Dockerfile 构建阶段 `ARG API_BASE_URL=http://api:8000`——
   解析不了的主机名会以「普通连接失败」的形式报错，那才是这个容错分支设计时的场景。
3. **`raw()`（`request()` 的兄弟，返回 Response）没做同样的错误归一化**，
   导致走 `raw()` 的 `getProducts`（sitemap 在用）根本没法被容错。
   现在它和 `request()` 一样把传输错误包成 `ApiError(NETWORK_ERROR)`。

修完后静态页生成从「卡死 180 秒」变成 **23 秒跑完 17/17**。

## 沙箱本地复现构建的坑

想本地复现 `next build` 验证，结果被沙箱的文件系统代理连着挡两次：
先 `SAFE_DELETE_BULK_CONFIRM_REQUIRED`（Next 清理 `.next`，2125 个条目 > 阈值 50），
再 `EEXIST: file already exists, mkdir '.next/server/pages'`。
`scripts/sandbox-fs-guard.cjs` 是给 `npm install` 用的，压不住 `next build`。
**结论：前端生产构建别在本地试，直接上服务器，日志写到 `/tmp/build.log` 再 grep。**


