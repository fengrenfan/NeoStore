# 2026-09-15

## 调研：GitHub 头部跨境独立站开源项目

用 GitHub API 实测（不抄榜单文章，那些数据互相矛盾），得到 star / 协议 / 语言 / 活跃度：

| # | 项目 | Stars | 语言 | 协议 | API |
|---|---|---|---|---|---|
| 1 | medusajs/medusa | 36,304 | TypeScript/Node | MIT(core)+EE | REST |
| 2 | bagisto/bagisto | 28,114 | PHP/Laravel | MIT | REST + GraphQL |
| 3 | saleor/saleor | 23,327 | Python/Django | BSD-3 | 仅 GraphQL |
| 4 | spree/spree | 15,697 | Ruby on Rails | BSD-3 | REST + TS SDK |
| 5 | vendurehq/vendure | 8,438 | TypeScript/NestJS | GPL-3 | GraphQL |

参考轻量路线：vercel/commerce 14,258（MIT）、yournextstore 5,498（MIT，Next.js+Stripe）。

**共性结论（本项目复用的核心）**
- 领域链路一致：Product → Variant → SKU → Price(按 Region/Channel) → Inventory(按 Warehouse)
  → Cart → Checkout → Order → Fulfillment → Return。
- 四层分层 + Admin 永远是独立第二个前端。
- 扩展机制只有三种：进程内 Plugin、进程外 App(webhook)、可替换 Module。
- 中间件标配：PostgreSQL + Redis + 专用搜索 + Docker Compose。
- 订单行必须快照商品名/价，不能外键回查（否则改价污染历史订单）——五家全这么做。
- 跨境三刚需：币种抽象成渠道/区域维度、i18n 原生字段或外挂 CMS、支付网关插件化。

## 新项目：NeoStore（轻量单店 DTC 跨境独立站）

用户确认的决策：单店 DTC 形态 / FastAPI + React / 先做通用多语言框架 /
方案 A 前端结构（Next.js 店面 + 独立 Vite 后台）/ PostgreSQL / 首期最小可用闭环。

设计文档已落地并提交：
`docs/superpowers/specs/2026-09-15-neostore-design.md`
git 已 init，首个 commit `60fd0bd`。

关键设计决定：
- i18n 用独立翻译表（`*_translation` + UNIQUE(entity_id, locale)），不用 JSONB 内嵌字段。
- 多币种：显式定价优先，缺失时按汇率换算兜底；换算结果按目标币种 decimal_places 取整。
- Region = 币种 + 语言集合 + 税率 + 可用支付方式的聚合。
- 金额一律 Decimal，计算收敛到 `pricing.calculate()` 单一入口。
- 库存用 `SELECT ... FOR UPDATE` 行锁 + reserved 预占防超卖。
- 下单接口支持 `Idempotency-Key` 防重复下单。
- domain 层禁止 import fastapi / 直接拿 Session，持久化走 repository 接口。

状态：**设计待用户评审**，评审通过后进入实施计划（M0–M4）。
