# 2026-09-17

## README 补齐（把上线之后的现实写进去）

原 README 写于 09-15 15:58，即**上线之前** —— 缺部署、运维、和后来加的两个验证脚本。
本次纯增量补 89 行、**一行未删**（138 → 227 行），章节结构：

`# NeoStore` → `## 现状`(新) → `## 目录`(扩展) → `## 起服务` → `## 线上部署`(新) →
`## 本地开发` → `## 常用命令`(扩展) → `## 环境变量` → `## 架构约束`(+子节) → `## 运维`(新) → `## 已知取舍`

具体补了什么：

- **现状**：M0–M4 落地、后端 150 passed / 1 skipped、已上线、未接真实支付网关；
  顺带写明种子商品图指向占位 CDN、前端会降级成渐变块（免得被当成 bug）。
- **线上部署**：`store.xiaodigua.shop` 四个入口（顾客端/后台/API/健康检查）、
  宿主 nginx → `127.0.0.1:8091` Caddy 按前缀分流、代码在 `/home/app/store`、
  `COMPOSE_PARALLEL_LIMIT=1` 串行构建、**`up -d` 不会因镜像变新而重建容器**（要 `--force-recreate`）、
  生产镜像 `NEXT_PUBLIC_API_BASE_URL` 故意留空的同源设计、`NEXT_PUBLIC_*` 是构建期内联。
- **运维**：三个 cron 任务的位置与行为（看门狗每 2 分钟 / 磁盘清理每周日 04:10 / MySQL 备份每天 03:00）；
  写明 `quant-*` 不在看门狗清单、看门狗只看容器状态不探 HTTP；
  并写死「磁盘清理永不用 `image prune -a`」的理由（基础镜像是离线构建的前提）。
- **常用命令**：补 `storefront-check.cjs`、`currency-repro.cjs`、`scripts/nginx/`、`docs/RUNBOOK.md`；
  标注 `storefront-check.cjs` 的 locale 参数陷阱 —— 它的第二个参数会改访问的 locale，
  但**断言文案写死中文**，传非 `zh-CN` 会整片假报失败（我自己踩过一次，9/15 那次）。
- **架构约束**下新增子节「顾客端：区域（币种）与语言怎么解析」，把三个坑固化进去：
  `searchParams` 在 standalone 运行时是 `undefined`、从 `"use client"` 模块导入的**值**在服务端
  是 `undefined`、cookie→URL 续接必须 307 不能 `rewrite`。同时写明
  **`request.cookies.get()` 本身没问题**（免得后人重复 09-16 那次误判）。

提交并推送（GitHub 直连不稳定，按 github-api-push 的 `push_via_api.py` 走，两个分支同步）。
